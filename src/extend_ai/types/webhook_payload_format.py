# This file was auto-generated by Fern from our API Definition.

import typing

from ..core import enum

T_Result = typing.TypeVar("T_Result")


class WebhookPayloadFormat(enum.StrEnum):
    """
    The format used to deliver webhook payloads.

    - `json` — the full payload is sent inline in the webhook request body.
    - `url` — large payloads are replaced with a signed download URL.
    """

    JSON = "json"
    URL = "url"
    _UNKNOWN = "__WEBHOOKPAYLOADFORMAT_UNKNOWN__"
    """
    This member is used for forward compatibility. If the value is not recognized by the enum, it will be stored here, and the raw value is accessible through `.value`.
    """

    @classmethod
    def _missing_(cls, value: typing.Any) -> "WebhookPayloadFormat":
        unknown = cls._UNKNOWN
        unknown._value_ = value
        return unknown

    def visit(
        self,
        json: typing.Callable[[], T_Result],
        url: typing.Callable[[], T_Result],
        _unknown_member: typing.Callable[[str], T_Result],
    ) -> T_Result:
        if self is WebhookPayloadFormat.JSON:
            return json()
        if self is WebhookPayloadFormat.URL:
            return url()
        return _unknown_member(self._value_)
