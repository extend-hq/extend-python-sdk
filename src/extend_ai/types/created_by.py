# This file was auto-generated by Fern from our API Definition.

from __future__ import annotations

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2
from ..core.unchecked_base_model import UncheckedBaseModel, UnionMetadata


class CreatedBy_User(UncheckedBaseModel):
    """
    The actor that created this resource.

    * `user` — the resource was created via the studio by a logged-in user. The user's `email` is exposed.
    * `api_key` — the resource was created via the API using an API key. The key's `name` is exposed.

    May be `null` for resources created before attribution.
    """

    type: typing.Literal["user"] = "user"
    email: str

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


class CreatedBy_ApiKey(UncheckedBaseModel):
    """
    The actor that created this resource.

    * `user` — the resource was created via the studio by a logged-in user. The user's `email` is exposed.
    * `api_key` — the resource was created via the API using an API key. The key's `name` is exposed.

    May be `null` for resources created before attribution.
    """

    type: typing.Literal["api_key"] = "api_key"
    name: str

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


CreatedBy = typing_extensions.Annotated[
    typing.Union[CreatedBy_User, CreatedBy_ApiKey], UnionMetadata(discriminant="type")
]
