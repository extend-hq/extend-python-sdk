# Specify files that shouldn't be modified by Fern

README.md
uv.lock

# Publish workflow — customized with stricter error handling
# Keep ci.yml listed so Fern doesn't regenerate it after the rename
.github/workflows/ci.yml
.github/workflows/publish.yml

# Protect patched core files
src/extend_ai/core/serialization.py

# Protect custom wrapper code
src/extend_ai/wrapper/
src/extend_ai/__init__.py
src/extend_ai/py.typed

# Protect custom tests
tests/custom/
tests/wrapper/
